function changePlaceholder(input, newPlaceholder) {
    input.placeholder = newPlaceholder; // Меняем плейсхолдер на новый
}

function resetPlaceholder(input, originalPlaceholder) {
    input.placeholder = originalPlaceholder; // Возвращаем плейсхолдер к исходному
}